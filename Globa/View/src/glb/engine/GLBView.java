package glb.engine;

public class GLBView{
    public static void main(String[] args) {
        // get all the necessary params via CLI 
        if(args.length>0){
            for (int i = 0; i < args.length; i++) {
                System.out.println(i+" "+args[i]);
            }
        }

        //get the tables metadata
        
        //generate the view
    }
}